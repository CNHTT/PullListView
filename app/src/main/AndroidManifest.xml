<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cnh.bluetooth">

    <application
        android:name=".BlueApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">

        </activity>
        <activity android:name=".view.SplashActivity" />
        <activity android:name=".view.activity.LoginActivity"></activity>

        <activity android:name=".view.activity.GoodsActivity"></activity>

        <activity android:name=".view.activity.NotificationActivity"><intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter></activity>

        <activity android:name=".view.activity.PhotoActivity"></activity>

        <activity android:name=".view.activity.PickerViewActivity"></activity>、


        <activity android:name=".view.activity.PullToZoomListViewActivity"></activity>

        <activity android:name=".view.activity.FadingScrollViewActivity">  </activity>

    </application>

</manifest>